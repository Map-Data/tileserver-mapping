"""
Django settings for tileservermapping project.

Generated by 'django-admin startproject' using Django 2.0.6.

For more information on this file, see
https://docs.djangoproject.com/en/2.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/2.0/ref/settings/
"""
import os

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.getenv('TM_SECRET_KEY', 'v€ry $ecret key')

# imported here because rest-frameworks schema generation requires it
from .settings_base import *

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.getenv('TM_DEBUG', '') != ''

ALLOWED_HOSTS += os.getenv('TM_HOSTS', 'localhost').split(',')

# Database
# https://docs.djangoproject.com/en/2.0/ref/settings/#databases
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'HOST': os.getenv('TM_DB_HOST', 'localhost'),
        'PORT': os.getenv('TM_DB_PORT', '5432'),
        'USER': os.getenv('TM_DB_USER', 'osm_tileservermapping'),
        'NAME': os.getenv('TM_DB_NAME', 'osm_tileservermapping'),
        'PASSWORD': os.getenv('TM_DB_PASSWORD', 'osm_tileservermapping')
    }
}

STATIC_ROOT = os.path.join('/', 'app', 'static')
MEDIA_ROOT = os.getenv('TM_MEDIA_ROOT', os.path.join('/', 'app', 'media'))
